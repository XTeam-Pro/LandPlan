[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    -v
    --tb=short
markers =
    unit: unit tests
    integration: integration tests
    e2e: end-to-end tests
